---
- hosts: all
  pre_tasks:
    - name: Verify Ansible version is high enough
      assert:
        that: "ansible_version.full is version_compare('2.5', '>=')"
        msg: >
          "You must update Ansible to at least 2.5 to run these playbooks"
